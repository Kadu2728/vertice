DATABASE_URL=postgresql+psycopg://postgres:CHANGE_ME@localhost:5432/vertice
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Banco separado do dev — os testes de integração rodam DROP/CREATE de
# schema no teardown; nunca aponte isso pro mesmo banco do DATABASE_URL.
TEST_DATABASE_URL=postgresql+psycopg://postgres:CHANGE_ME@localhost:5432/vertice_test
